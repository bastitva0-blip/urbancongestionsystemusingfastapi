# API
fastapi==0.111.0
uvicorn[standard]==0.29.0
httpx==0.27.0

# Database
sqlalchemy==2.0.30
asyncpg==0.29.0
alembic==1.13.1
geoalchemy2==0.15.1

# Config
pydantic-settings==2.2.1
pydantic==2.7.1

# ML
scikit-learn==1.4.2
numpy==1.26.4
joblib==1.4.2

# Auth + Rate limiting
slowapi==0.1.9
passlib[bcrypt]==1.7.4

# Logging
structlog==24.1.0

# Monitoring
# NOTE: prometheus-fastapi-instrumentator 6.x/7.x/8.x all have a routing bug
# with FastAPI 0.111+ include_router (_IncludedRouter lacks .path).
# The conftest.py monkey-patch resolves this for tests.
# In production (Docker) the middleware works correctly because all routes are
# registered at startup before the middleware processes any real requests.
prometheus-fastapi-instrumentator==6.1.0
prometheus-client==0.19.0

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6
pytest-cov==5.0.0

# Load testing (install separately)
# locust==2.24.1
